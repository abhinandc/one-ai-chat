# OneEdge Environment Variables Template
# Copy this file to .env and fill in your actual values
# NEVER commit .env to git - it's in .gitignore

# Google OAuth (Get from Google Cloud Console)
VITE_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
VITE_GOOGLE_REDIRECT_URI=https://your-domain.com/oauth2/callback

# Supabase Configuration (Get from Supabase Project Settings)
VITE_SUPABASE_URL=https://your-project-id.supabase.co
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key

# SECURITY NOTES:
# - Service key should ONLY be in .env.local (NOT in .env.example)
# - Service key should ONLY be used in Edge Functions, NEVER in frontend code
# - Rotate all keys if .env was ever committed to git
# - Use environment-specific files (.env.production, .env.staging, etc.)

# Optional: API Proxy URL
# VITE_API_PROXY_URL=https://your-api-proxy.com

# Optional: ElevenLabs (for Sia voice assistant)
# VITE_ELEVENLABS_API_KEY=your-elevenlabs-api-key
